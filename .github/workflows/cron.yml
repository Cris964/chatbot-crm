name: CRM Cron Jobs

on:
  schedule:
    # Ejecuta el Remarketing cada 4 horas
    - cron: '0 */4 * * *'

jobs:
  run-remarketing:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Remarketing Endpoint
        run: |
          curl -s -X GET "https://chatbot-crm-xi.vercel.app/api/cron/remarketing?token=n3xus_cron_2026"
