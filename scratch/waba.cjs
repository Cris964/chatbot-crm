const { createClient } = require("@supabase/supabase-js"); require("dotenv").config(); const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); async function run() { const { data } = await supabase.from("clients").select("*").ilike("name", "%samaritana%"); const client = data[0]; const token = client.whatsapp_token || process.env.WHATSAPP_TOKEN; const wabaId = "866828142953786"; const templatesRes = await fetch(`https://graph.facebook.com/v21.0/${wabaId}/message_templates?access_token=${token}`); const templatesJson = await templatesRes.json(); console.log("Templates:", templatesJson.data ? templatesJson.data.map(t => ({ name: t.name, language: t.language, status: t.status })) : templatesJson); } run();
