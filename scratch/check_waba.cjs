const { createClient } = require("@supabase/supabase-js"); require("dotenv").config(); const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); async function run() { const { data } = await supabase.from("clients").select("name, whatsapp_token").ilike("name", "%samaritana%"); const client = data[0]; const res = await fetch(`https://graph.facebook.com/v21.0/866828142953786/message_templates?access_token=${client.whatsapp_token}`); const json = await res.json(); console.log(JSON.stringify(json.data ? json.data.map(t => ({name: t.name, language: t.language, status: t.status})) : json, null, 2)); } run();
