const { createClient } = require("@supabase/supabase-js"); require("dotenv").config(); const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); async function run() { const { data } = await supabase.from("clients").select("*").ilike("name", "%samaritana%"); if (!data || data.length === 0) { console.log("Client not found"); return; } const client = data[0]; const token = client.whatsapp_token || process.env.WHATSAPP_TOKEN; const phoneId = client.phone_number_id || process.env.WHATSAPP_PHONE_ID; console.log("Using Token:", token.substring(0, 15) + "..."); console.log("Using Phone ID:", phoneId); const res = await fetch(`https://graph.facebook.com/v21.0/${phoneId}?fields=whatsapp_account_id&access_token=${token}`); const json = await res.json(); console.log("Phone Data:", json); if (json.whatsapp_account_id) { const wabaId = json.whatsapp_account_id; const templatesRes = await fetch(`https://graph.facebook.com/v21.0/${wabaId}/message_templates?access_token=${token}`); const templatesJson = await templatesRes.json(); console.log("Templates:", templatesJson.data ? templatesJson.data.map(t => ({ name: t.name, language: t.language, status: t.status })) : templatesJson); } } run();
