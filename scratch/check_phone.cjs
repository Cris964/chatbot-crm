const { createClient } = require("@supabase/supabase-js"); require("dotenv").config(); const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); async function run() { const { data } = await supabase.from("clients").select("name, phone_number_id, whatsapp_token").ilike("name", "%samaritana%"); const client = data[0]; const res = await fetch(`https://graph.facebook.com/v21.0/${client.phone_number_id}?access_token=${client.whatsapp_token}`); const json = await res.json(); console.log(json); } run();
