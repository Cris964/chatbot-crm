import { createClient } from @supabase/supabase-js; import dotenv from dotenv; dotenv.config(); const sb = createClient(process.env.VITE_SUPABASE_URL, process.env.VITE_SUPABASE_ANON_KEY); async function test(){ const {data} = await sb.from(conversations).select(*).not(messages, is, null).limit(3); console.log(JSON.stringify(data, null, 2)); } test();
